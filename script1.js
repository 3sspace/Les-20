let number = prompt(
    "Enter number"
    );
let a = number;
function revers(number){
     if (a <= 0) {
     return a = a*(-1);
} else if (a > 0) {
     return a = a*(-1);
} 
    else {
     return 'Enter correct number';
} 
}
alert(revers(number));
